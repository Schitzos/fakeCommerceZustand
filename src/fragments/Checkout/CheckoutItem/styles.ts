import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  base: {
    gap: 4,
    flex: 1,
  },
  checkoutItem: {
    gap: 8,
    flex: 1,
  },
  image: {
    width: 50,
    height: 50,
  },
  productInfoCount: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
    flex: 1,
  },
});
